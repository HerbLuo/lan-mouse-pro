# LEADER 状态 — 2026-09-14 01:30

> LEADER 维护。compact 后 / 跨 session 第一件事 Read 本文件恢复进度。
> 每次状态变更（PLAN 切换 / STEP 完成 / validator 跑完 / milestone 完成 / 切换里程碑）更新。

---

## 当前 PLAN

`next/PLAN-2.1-CLIPBOARD.md` — **跨设备文件接收（auto-accept）+ GUI 配置 + CLI + 剪贴板回灌**（M4 + M5；M0a / M0b / M0c / M1a / M1b / M2a / M2b / M3a ✅ 已完成；M4 ⏸️ 等用户真机验证 M3a 后启动）

> 用户 2026-09-13 决策（PLAN 拆分 — commit `bdb5282`）：
> - 原 `PLAN-2-CLIPBOARD.md` 中的 M3b（8 STEPs ~11.5h）+ M4 合并方案 → 拆分为 **`PLAN-2.1-CLIPBOARD.md` M4（3 STEPs ~4.5h）+ M5（5 STEPs ~7.0h）**，共 ~11.5h
> - M4 = "IPC `ClipboardConfig` schema + 三平台 `ClipboardBackend::set_files` trait + 接收端剪贴板回灌"
> - M5 = "拔网处理 + 端到端性能 + Vue IPC 绑定 + GUI 配置 + CLI"
> - step 映射（保留 audit trail）：`3b.1 → 4.1` / `3b.7a → 4.2` / `3b.7b → 4.3` / `3b.2 → 5.1` / `3b.3 → 5.2` / `3b.4 → 5.3` / `3b.5 → 5.4` / `3b.6 → 5.5`
> - 切换 PLAN 时重置"累计执行时间 / 上次 validator 位置"

> 已完成：PLAN-1（多屏友好的被控端定位）— M0-M3 全完成、M4 暂延（用户决策 2026-09-08 19:30）；PLAN-2（M0a-M3a）— 全部 ✅。

---

## 当前里程碑

**M3a — 复制文件 + HTTP/3 transfer（✅ 已完成 5/5 STEP + 整批 validator PASS-with-followup + ✅ 用户真机验证 PASS）**

> M3a 5 STEP：3a.1 ✅ / 3a.2 ✅ / 3a.3 ✅ / 3a.4 ✅ / 3a.5 ✅
> M3a 实际 ~6.5h AI（PLAN §3 估时 9h AI 内）
> M3a 整批 validator（`next/STEP-VALIDATION-P2-M3a-FULL.md`；commit `49fd3c7`）✅ PASS-with-followup（0 P0 / 0 P1 / 5 P2 / 1 P3）

> 2026-09-13 12:30：**✅ 用户真机验证 PASS**（200 MiB 双向 + 取消 + 6 项人类测试矩阵全过）
> **leader 启动 M4 STEP-4.1**

**M4 — IPC `ClipboardConfig` + 平台 `set_files` + 剪贴板回灌（✅ DONE — 3/3 STEP + validator PASS-with-followup + ✅ 用户真机验证 PASS（macOS / Linux / Windows hotfix）**）**

> M4 3 STEP：4.1 ✅ / 4.2 ✅ / 4.3 ✅
> M4 累计 ~155 min AI（PLAN §4 估时 4.5h 内）
> M4 整批 validator（`next/STEP-VALIDATION-P2-M4-FULL.md`；commit `6fbca59`）✅ PASS-with-followup（0 P0 / 0 P1 / 2 P2；12 ⚠️ PLAN 偏差全部 A1 策略）
> **M4 里程碑交付 4 项全部到位**（macOS / Linux / Windows 真机验证 PASS）：
> - IPC `ClipboardConfig` 8 字段
> - `ClipboardBackend::set_files` 三平台
> - 接收端剪贴板回灌 + 4 skip + pre-stamp
> - TOML `inject_to_clipboard`
>
> 2026-09-13 18:30：**✅ 用户 M4 真机验证 PASS（macOS / Linux / Windows 三平台）**
> Windows BUG #S-12 用户 hotfix 修复（3 commit：0c9ae6c / 6de48cc / 608e51a）
> - **真根因**：loopback pre-stamp 用 batch_fingerprint 而非 local fingerprint → A → B → A' → B' 死循环 → 3.5s 无 pong → 强制断连 → STATUS_ACCESS_VIOLATION
> - 修复在 `src/service.rs::maybe_inject_files_to_clipboard` pre-stamp 改用 `file_selection_fingerprint(&paths)`；windows.rs set_files 配套清理
> - 测试覆盖盲点教训已记入 SUGGESTION-FIXED #S-12（建议 M5 / post-M5 加 maybe_inject_files_to_clipboard e2e 测试）
> **leader 启动 M5 STEP-5.1**

> 3 STEPs：
> - **4.1** IPC `ClipboardConfig` 扩展 + 早拒绝 PopupGuard 串通（drop `auto_accept_files`；8 字段 `enabled` / `accept_dir: PathBuf` / `ignore_*` ×3 / `max_file_size` / `keep_partial` / `inject_to_clipboard`）
> - **4.2** `ClipboardBackend::set_files` trait + 三平台实现（macOS NSPasteboard / Windows CF_HDROP / Linux URI list）—— **planer round 2 审阅拆步**：M3a `commit 69ebd9a` 仅含只读 `current_files()` + `watch_files()`，无 `set_files`；原 STEP-3b.7 拆为 4.2（trait）+ 4.3（集成）
> - **4.3** 接收端剪贴板回灌 + skip conditions + 防回环 + IPC 集成（依赖 4.2）

**M5 — 拔网处理 + 端到端性能 + Vue IPC 绑定 + GUI 配置 + CLI（✅ DONE — 5/5 STEP + 闸 3 M5 收尾全套绿 + final validator PASS-with-followup；~250 min ≈ 4.2h AI；⏸️ 用户真机验证 gate）**

> M5 5 STEP：5.1 ✅ / 5.2 ✅ / 5.3 ✅ / 5.4 ✅ / 5.5 ✅
> M5 累计 ~250 min AI（PLAN §4 估时 M5 ~7.0h 内）
> M5 闸 3 全套绿（cargo build / test / clippy / fmt）；workspace 512 tests pass / 0 fail / 19 ignored
> M5 final validator（`next/STEP-VALIDATION-P2-M5-FULL.md`；commit `a3f233c`）✅ PASS-with-followup（0 P0 / 0 P1 / 0 P2 / 1 P3 docs；7 ⚠️ PLAN 偏差全部 A1）
> **M5 milestone DONE** — 7 项交付全部到位（拔网 / 性能 / cancel / keepalive / GUI / per-peer / CLI）

> 5 STEPs：
> - **5.1** 拔网处理：HTTP/3 client stream error → IPC 推 `FrontendEvent::FileTransferFailed`
> - **5.2** 端到端性能 + 收尾（100 Mbps LAN < 30s + Wi-Fi < 60s + cancel + 拔网 双向 + keepalive↔idle race + Pong ≤ 600ms）
> - **5.3** Vue 类型 + IPC 绑定（drop `FileTransferRequest` 类型；新增 `ClipboardConfigChanged` IPC 事件）
> - **5.4** GeneralPanel + per-peer UI（`enabled` / `accept_dir` / `ignore_*` / `max_file_size` / `keep_partial` / **`inject_to_clipboard`** + per-peer `enable_clipboard_to`）+ TOML 落盘
> - **5.5** CLI 集成（`lan-mouse-cli SetClipboardConfig` / `SetEnableClipboardTo`）

**M2b — 剪贴板图片 + Windows / Linux（✅ 已完成 + 整批 validator 0 P0/P1 PASS-with-followup + 用户真机已通）**

> M2b 4 STEP + 1 cleanup：2b.1 ✅ / 2b.2 ✅ / 2b.3 ✅ / 2b.4 ✅ / 2b-CLEANUP ✅
> M2b validator 整批审 PASS-with-followup（0 P0 / 1 P1 / 0 P2 / 0 P3；`next/STEP-VALIDATION-P2-M2b.md`；commit `0ea9118`）
> P1.1 / P1.2 已在 `0cfcab4` 修复（cache pollution + HGLOBAL leak）

**M2a — 剪贴板图片基础设施 + macOS（✅ 已完成 + 整批 validator PASS-with-followup + 用户真机已通）**

### Post-M2 BUGFIX（用户真机驱动，**无需 validate**）

> 用户 2026-09-10 真机出现 screenshot mouse-stuck bug；2026-09-12 真机 image copy-paste 测试通过。
> 用户决策 2026-09-12：post-M2 BUGFIX 全套真机已通，正式进入 M3，**不**触发 validator 整批审。

- ✅ `c8a87f8` docs(known-bugs): record 2026-09-10 screenshot mouse-stuck bug
- ✅ `a94c249` fix(service): move clipboard polling into spawn_local task
- ✅ `fd5737d` fix(service): route poller cmd arm + inbound helpers through async paths
- ✅ `8de4219` fix(service): move inbound image apply to spawn_local task
- ✅ `fc38296` fix(service): move inbound HTTP/3 GET into spawn_local task (round 2)
- ✅ `8df5ec2` fix(service): code-review follow-up
- ✅ `257c939` chore(logs): promote 3 clipboard-flow debug lines to info
- ✅ `b4191d4` fix(quic/clipboard): stream priorities + macos changeCount image cache
- ✅ `4313940` `420221a` fix + revert（spawn_blocking regression）
- ✅ `df3076b` fix(clipboard/macos): decode raw CF_DIBV5 via prepend_bmp_file_header
- ✅ `ff07a1b` fix(clipboard/macos): add JPEG pasteboard probe
- ✅ `aaec0e6` fix(clipboard/windows): publish both CF_DIBV5 and CF_DIB
- ✅ `a353255` `0d5cd3f` fix + revert (RGBA→24-bit BI_RGB)
- ✅ `73ab5da` fix(windows/service): EmptyClipboard before SetClipboardData + image LRU loopback
- ✅ `6c4f023` fix(emulation): forward ClipboardImage to inbound dispatcher
- ✅ `aa59a77` fix(service): image-first dispatcher order
- ✅ `1f23710` fix(clipboard/windows): realign windows-sys 0.61 import paths

### M3 → M4 接力点（2026-09-13）

- 2026-09-12 23:00：用户决策 M2 真机已通，正式进入 M3 阶段
- 2026-09-13 04:50：M3a 整批完成交付（21 commit since `a18dd5c` baseline）+ M3a 整批 validator PASS-with-followup（commit `49fd3c7`）
- 2026-09-13 12:00：用户决策 PLAN 拆分 — M3b 拆为 M4 + M5，新文件 `next/PLAN-2.1-CLIPBOARD.md` 落地（commit `bdb5282`）
- 2026-09-13 12:09：当前 HEAD = `bdb5282`（docs: PLAN 拆分）
- 2026-09-13 12:15：**⏸️ 用户 M3a 真机验证 gate** —— leader 暂停派发，等用户介入验证；M4 STEP-4.1 待 gate 通过后启动
- 2026-09-13 12:30：**✅ 用户 M3a 真机验证 PASS**（6 项人类测试矩阵全过）→ leader 启动 M4 STEP-4.1：派 `plan-step-executor` 直接执行

---

## 当前累计执行时间（自上次 validator 审阅起）

**0 min**（M5 final validator 整批审于 commit `a3f233c` 跑完，重置计时）
> ⏸️ 用户 M5 真机验证 gate —— leader 暂停派发

> 触发 validator 的两个条件（OR）：
> - 累计 > 1 小时
> - 当前里程碑结束（next milestone 全部 STEP 完成 + post-next hot-fix 处理完）
>
> 下一里程碑触发时机：next milestone N STEP 全完成 或 累计 > 1h

---

## 上次 validator 审阅位置

**M3a FULL 整批**（commit `1d4e014` 3a.1-3a.2 + `4287e6a` 3a.3 + `49fd3c7` FULL）✅ PASS-with-followup（`next/STEP-VALIDATION-P2-M3a-FULL.md`；0 P0 / 0 P1 / 5 P2 / 1 P3）

> 全部前次 P1 followup（cache insert / test coverage / SUGGESTION 还原 / path traversal）已 RESOLVED
> P2 followups 待 M4 STEP-4.1 / M5 STEP-5.1 carry-forward 评估（5 项 P2 中 P2.1 cache-before-broadcast + P2.3 post-write fsync 是 M5 STEP-5.1 候补；P2.2 poisoned-mutex + P2.5 silent log 是 M4 cleanup 候补；P2.4 default_accept_dir 已被 M4 4.1 IPC schema 解决）

> M2b 整批（`0ea9118`）✅ PASS-with-followup（`next/STEP-VALIDATION-P2-M2b.md`）
> M2a 整批（`a74bd7d`）✅ PASS-with-followup
> M1b 整批（`395a05b`）✅ PASS-with-followup
> M1a 整批 + 10 个 post-M1a hot-fix ✅ PASS-with-followup
> M0c 整批（`b834cfa`）✅ PASS-with-followup
> M0b 整批（`fdf03bc`）✅ PASS-with-followup
> M0a 整批（`a032e75`）✅ PASS-with-followup

---

## 待办 / 决策项

### 用户决策（PLAN-2 启动后）

- [x] **用户**：启动 PLAN-2 → ✅ 2026-09-08 20:00
- [x] **用户**：M2b P1.1+P1.2 修复 → ✅ commit `0cfcab4`
- [x] **用户**：post-M2 BUGFIX（19 commit）是否触发 validator / 直接进 M3 → ✅ **直接进 M3，2026-09-12 决策，不触发 validator**
- [x] **用户**：M3a validator PASS / FAIL 决策 → ✅ PASS-with-followup（`49fd3c7` 整批审）
- [x] **用户**：PLAN 拆分（M3b → M4 + M5 + 新文件 `PLAN-2.1-CLIPBOARD.md`）→ ✅ 2026-09-13 commit `bdb5282`

### 用户决策（待办）

- [x] **用户**：M3a 真机验证清单（PLAN-2 §8 M3a 人类测试矩阵）→ ✅ PASS（6 项全过；2026-09-13 12:30）
- [x] **用户**：PLAN 拆分（M3b → M4 + M5 + 新文件 `PLAN-2.1-CLIPBOARD.md`）→ ✅ 2026-09-13 commit `bdb5282`
- [x] **用户**：M3a 真机验证清单（PLAN-2 §8 M3a 人类测试矩阵）→ ✅ PASS（6 项全过；2026-09-13 12:30）
- [x] **用户**：PLAN 拆分（M3b → M4 + M5 + 新文件 `PLAN-2.1-CLIPBOARD.md`）→ ✅ 2026-09-13 commit `bdb5282`
- [x] **用户**：M4 真机验证清单（PLAN-2.1 §8 M4 人类测试矩阵）→ ✅ PASS（macOS / Linux / Windows 三平台；Windows BUG #S-12 用户 hotfix 修复；2026-09-13 18:30）
- [ ] **用户**：M5 真机验证清单（PLAN-2.1 §8 M5 矩阵：拔网双向 + 200 MiB 性能 + cancel + keepalive↔idle + GUI 8 控件 + per-peer + CLI 4 invocation）→ 验证 PASS 后用户对齐下一里程碑 → leader 续约或交班
- [ ] **用户**：候选下一里程碑决策（PLAN §6 out of scope：剪贴板格式协商 / 文件断点续传 / Wayland portal / 可观察卡片 / 文档章节 / 项目收尾）→ 由用户决定是否立 PLAN-3

### Hot-fix（用户本地记录）

> 用户在 `next/.LEADER.md` 维护 hot-fix v4/v5 记录。

---

## 已完成 STEP（PLAN-2）

### M0a — ProtoEvent codec 双轨化（✅ 已完成，4 commit）

| Commit | 标题 |
|---|---|
| `a71340a` | chore(fmt): whitespace fold + .clone() on ProtoEvent de-Copy |
| `248d276` | feat(proto): dual-track ProtoEvent codec + clipboard / file-transfer variants |
| `f21ae81` | docs: add STEP-P2-M0a report |
| `a032e75` | docs: record M0a completion |

### M0b — HTTP/3 基础（✅ 已完成，6 commit）

| Commit | 标题 |
|---|---|
| `f66c0f7` | feat(quic): HTTP/3-lite over bare QUIC bidi streams |
| `8729f9f` | chore(deps): add bytes / h3 / h3-quinn / rand for M0b HTTP/3 spike |
| `ff2f55c` | docs(suggestion): record Path 1 → Path 2 decision rationale |
| `98dd971` | docs(quic): update Channel enum + route_input doc + STEP-P2-M0b-0.2 report |
| `9acdf21` | feat(quic): http3 server accept loop + Http3Client helper |
| `fdf03bc` | docs: add STEP-P2-M0b-0.3-0.4 report |

### M0c — StreamC 接通 + IPC 扩展（✅ 已完成，7 commit）

| Commit | 标题 |
|---|---|
| `371eee0` | feat(quic): wire StreamC reader + http3 server + ClipboardConfig IPC |
| `47a4e03` | fix(quic): cap http3 server concurrent in-flight requests at 32 |
| `44dbd60` | docs: add STEP-P2-M0c-0.5-0.6 report |
| `3d5ae1f` | docs: archive STEP-VALIDATION-P2-M0b |
| `7282756` | docs: add STEP-P2-M0c-0.7 report |
| `86781b0` | chore(capture): demote post-Leave Ack warning to trace level |
| `b834cfa` | docs: archive STEP-VALIDATION-P2-M0c |

### M1a — 剪贴板小文本 ≤ 1 KiB 同步（✅ 已完成，5 STEP + 10 hot-fix）

### M1b — 剪贴板大文本 + 防回环加固（✅ 已完成，4 STEP + cleanup）

### M2a — 剪贴板图片基础设施 + macOS（✅ 已完成，5 commit + cleanup）

### M2b — 剪贴板图片 + Windows / Linux（✅ 已完成，5 STEP + cleanup）

### M3a — 复制文件 + HTTP/3 transfer（✅ 完成 5/5 STEP + 整批 validator PASS-with-followup）

| Commit | 标题 |
|---|---|
| `3f13c2b` | feat(clipboard): file metadata collection + streaming sha256 + tests (3a.1) |
| `a77bb72` | chore(deps): tokio fs feature + tempfile dev-dep for file meta collection |
| `0b78c33` | docs: archive M3a 3a.1 — file metadata collection + streaming sha256 |
| `69ebd9a` | feat(clipboard): file trait surface + macOS/Windows/Linux current_files impl (3a.2 partial) |
| `9a766c9` | chore(deps): add notify-rust = "4" for desktop popups |
| `5cb6a01` | chore(fmt): rustfmt sweep across post-M2 BUGFIX drift + clippy hints |
| `4c48fd2` | feat(clipboard): file_meta max_size + ExceedsLimit + collect_files_blocking |
| `c92e567` | feat(clipboard): file_cache 1 GiB byte-budget LRU keyed by sha256 |
| `13af40b` | feat(popup): notify-rust PopupGuard + PopupKind taxonomy |
| `1cc2808` | fix(lib): register popup module at crate root |
| `f2936e7` | feat(service): dispatch_files + spawn_blocking sha256 + StreamC push |
| `54ce992` | docs(suggestion): track M3a STEP-3a.2 follow-ups #S-5 / #S-6 |
| `35956c5` | docs: archive STEP-P2-M3a-3a.2 |
| `1d4e014` | docs: archive STEP-VALIDATION-P2-M3a-3a.1-3a.2 (PASS-with-followup) |
| `be34c7c` | feat(clipboard/file_cache): split insert into insert_owned + insert_returning_prev (P1.1/P2.3) |
| `af0e685` | feat(service): dispatch_files inserts file bytes into file_cache via spawn_blocking (P1.1) |
| `bb849a6` | test(service): dispatch_files covers 5 early-return branches (P1.2) |
| `81983a9` | chore(poller): document image-first probe suppresses file probe (P2.2) |
| `64ef0ad` | docs(suggestion): re-add #S-5 / #S-6 entries (P1.3) |
| `36b912b` | feat(service): handle_clipboard_inbound_files wires receiver-side flow (3a.3) |
| `21c729e` | docs: archive STEP-P2-M3a-3a.3 + SUGGESTION #S-7/#S-8/#S-9 |
| `4287e6a` | docs: archive STEP-VALIDATION-P2-M3a-3a.3 (PASS-with-followup) |
| `347c6b6` | fix(service): sanitize filename against path traversal in resolve_unique_path (P1.A) |
| `fea08ae` | docs(next): archive P1.A path traversal fix dispatch |
| `7aa2bc7` | feat(quic): HTTP/3 /clipboard/file route streams from file_cache (3a.4) |
| `1f84b3a` | feat(service): wire file_cache into LanMouseListener + LanMouseConnection (3a.4) |
| `31c81f9` | docs: archive STEP-P2-M3a-3a.4 |
| `5d6ef68` | feat(service): dispatch_files fires FileTransferCancel on supersede (3a.5) |
| `c899b22` | docs(suggestion): track M3a STEP-3a.5 follow-up #S-10 |
| `b446ab0` | docs: archive STEP-P2-M3a-3a.5 |
| `49fd3c7` | docs: archive STEP-VALIDATION-P2-M3a-FULL (PASS-with-followup) |

**M3a validator FULL 整批审**（commit `49fd3c7`）：✅ PASS-with-followup（0 P0 / 0 P1 / 5 P2 / 1 P3）

---

## PLAN-2.1 里程碑路线图

### M4 — IPC `ClipboardConfig` + 平台 `set_files` + 剪贴板回灌（✅ DONE；macOS / Linux 真机 PASS；Windows BUG #S-12 deferred）

| STEP | 估时 | 任务 | 状态 |
|---|---|---|---|
| **4.1** | **1.5h** | IPC `ClipboardConfig` 扩展 + 早拒绝 PopupGuard 串通（drop `auto_accept_files`；8 字段 + `Service::max_file_size()` getter + TOML `[clipboard]` 段加 `inject_to_clipboard`） | ✅ ~45 min（4 commit） |
| **4.2** | **1.5h** | `ClipboardBackend::set_files` trait + 三平台实现（macOS NSPasteboard `NSFilenamesPboardType` / Windows `CF_HDROP` / Linux `text/uri-list`） | ✅ ~55 min（4 commit；Windows hotfix `6de48cc` 修复） |
| **4.3** | **1.5h** | 接收端剪贴板回灌 + skip conditions + 防回环 + IPC 集成（依赖 4.2；4 类 skip：inject_to_clipboard=false / 回环命中 / 落盘失败 / forward-compat MIME_TOO_LARGE） | ✅ ~55 min（2 commit） |

### M5 — 拔网处理 + 端到端性能 + Vue IPC 绑定 + GUI 配置 + CLI（✅ DONE；⏸️ 用户真机验证 gate）

| STEP | 估时 | 任务 | 状态 |
|---|---|---|---|
| **5.1** | **1.5h** | 拔网处理：HTTP/3 stream error → IPC 推 `FrontendEvent::FileTransferFailed { sha256, reason, ts_ms }` + `.partial` 默认删除（`keep_partial` 控制） | ✅ ~85 min（3 commit；关闭 SUGGESTION P2.3） |
| **5.2** | **1.5h** | 端到端性能 + 收尾（100 Mbps LAN < 30s + Wi-Fi < 60s 双向 + cancel + 拔网 双向 + keepalive↔idle race + Pong ≤ 600ms） | ✅ ~50 min（4 commit；真机测试由用户执行） |
| **5.3** | **1.5h** | Vue 类型 + IPC 绑定（drop `FileTransferRequest`；新增 `ClipboardConfigChanged` IPC 事件 + `store.lastClipboard*` 三字段） | ✅ ~40 min（6 commit） |
| **5.4** | **1.5h** | GeneralPanel + per-peer UI（8 控件 + `inject_to_clipboard` checkbox + `enable_clipboard_to` per-peer）+ TOML 落盘 | ✅ ~50 min（6 commit；SUGGESTION #S-7 关闭） |
| **5.5** | **1.0h** | CLI 集成（`lan-mouse-cli set-clipboard-config` 含 `--inject-to-clipboard` / `set-enable-clipboard-to`） | ✅ ~25 min（3 commit：deps + cli + docs） |

| STEP | 估时 | 任务 | 状态 |
|---|---|---|---|
| **5.1** | **1.5h** | 拔网处理：HTTP/3 stream error → IPC 推 `FrontendEvent::FileTransferFailed { sha256, reason, ts_ms }` + `.partial` 默认删除（`keep_partial` 控制） | ✅ ~85 min（3 commit） |
| **5.2** | **1.5h** | 端到端性能 + 收尾（100 Mbps LAN < 30s + Wi-Fi < 60s 双向 + cancel + 拔网 双向 + keepalive↔idle race + Pong ≤ 600ms） | ✅ ~50 min（4 commit；真机测试由用户执行） |
| **5.3** | **1.5h** | Vue 类型 + IPC 绑定（drop `FileTransferRequest`；新增 `ClipboardConfigChanged` IPC 事件 + `store.lastClipboard*` 三字段） | ✅ ~40 min（6 commit） |
| **5.4** | **1.5h** | GeneralPanel + per-peer UI（7 控件 + `inject_to_clipboard` checkbox + `enable_clipboard_to` per-peer）+ TOML 落盘 | ✅ ~50 min（6 commit；SUGGESTION #S-7 关闭） |
| **5.5** | **1.0h** | CLI 集成（`lan-mouse-cli SetClipboardConfig` 含 `--inject-to-clipboard` / `SetEnableClipboardTo`） | ✅ ~25 min（3 commit：deps + cli + docs） |

> **PLAN-2.1 合计 ~11.5h AI**（M4 ~4.5h + M5 ~7.0h）

---

## 已完成 STEP（PLAN-1 历史摘要；不展开）

> PLAN-1（多屏友好的被控端定位）已收尾：M0-M3 全完成、M4 暂延（用户决策 2026-09-08 19:30）。

---

## 下一步

**2026-09-13 12:30** — ✅ 用户 M3a 真机验证 PASS（6 项人类测试矩阵全过）

**当前任务**：⏸️ 用户 M5 真机验证 gate —— leader 暂停派发，等用户介入验证

**M5 final validator carry-forward**（1 P3 docs cosmetic，不立 SUGGESTION）：
- **P3**：某处文档计数文案小误差（executor 报告内 16 vs 15 + 2 IPC round-trip 实际为 17 new tests in service.rs，与 commit `2629d72` 实际新增一致；不影响代码）

**PLAN-2.1 累计交付**（M4 + M5 = ~8.7h AI；PLAN §4 估时 ~11.5h 内）：
- M4（4.1 + 4.2 + 4.3）~155 min
- M5（5.1 + 5.2 + 5.3 + 5.4 + 5.5）~250 min
- M4 整批 validator PASS-with-followup（commit `6fbca59`）
- M5 partial validator（5.1+5.2）PASS-with-followup（commit `87203b0`）
- M5 final validator（5/5 STEPs）PASS-with-followup（commit `a3f233c`）

**用户 M5 真机验证清单**（PLAN §8 M5 人类测试矩阵）：
- macOS / Windows / Linux 各跑 CLI 子命令（4 项 invocation）→ config.toml diff
- GUI 配置 + per-peer `enable_clipboard_to` + `inject_to_clipboard` 切换
- 拔网双向（A→B + B→A）→ FileTransferFailed toast
- 200 MiB 性能（有线 LAN < 30s + Wi-Fi < 60s 双向）—— STEP-5.2 真机已验证
- keepalive↔idle race + Pong ≤ 600ms —— STEP-5.2 真机已验证
- cancel 双向 —— M3a STEP-3a.5 真机已验证
- 关掉 `inject_to_clipboard` checkbox 后复制文件 → 落盘但不入剪贴板

**用户对齐下一里程碑前**：
- ✅ 验证 M5 7 项真机通过
- ✅ 决策候选下一里程碑（PLAN §6 out of scope：剪贴板格式协商 HTML/RTF / 文件断点续传 / Wayland portal / 任何前端框架切换 / M5 之外的可观察卡片 / 剪贴板回灌 UI 提示 / M5 之外的 README.md / DOC.md 文档章节 —— 由用户决策是否立后续 PLAN）
- 候选 PLAN：PLAN-3 剪贴板格式协商 / PLAN-3 文件断点续传 / PLAN-3 Wayland portal / 项目收尾

**M5 5.1+5.2 validator carry-forward**（2 P2 文档计数，不阻塞 M5）：
- **P2-1**：executor 报告 §5 测试统计 "16 新单测" 文案误差（实际为 15 new tests in service.rs + 2 IPC round-trip = 17 new；与 commit `2629d72` 实际新增一致；不影响代码）
- **P2-2**：executor 报告 §6 累计执行时间文案 "M5 STEP-5.1 ~85 min + STEP-5.2 ~50 min = 135 min"（与 LEADER-STATE 一致；但 5.1 实际 85 min 包含 4 单测 + 4 commit split + leader state sync；估算略偏高）

> P2 留给后续 STEP（M5 5.3+）由 executor 顺手清理；不立 SUGGESTION 条目

**post-M5 hotfix 队列**：
- （已清空：Windows BUG #S-12 用户 2026-09-13 hotfix 修复，commit `0c9ae6c` / `6de48cc` / `608e51a`；closure 落地 `ca779da`）

**M4 整批审 carry-forward**（2 P2 不阻塞，可由 M5 executor 顺手清理）：
- **P2-1**：`src/service.rs:5897-5901` `handle_clipboard_inbound_files_decide` doc-comment 自相矛盾
- **P2-2**：`src/service.rs:6481, 4051-4059` `ReinjectSkipReason::NoLandedPaths` 变体定义但永不构造

**M4 完成交付清单**（10 commit since `49fd3c7` baseline；HEAD = `6fbca59`）：
- ✅ IPC `ClipboardConfig` 8 字段扩展（含 `inject_to_clipboard`） + drop `auto_accept_files` + accept_dir 升级为 required `PathBuf`
- ✅ `ClipboardBackend::set_files(&mut self, &[PathBuf])` trait + macOS NSPasteboard `NSFilenamesPboardType` / Windows `CF_HDROP` + DROPFILES / Linux `text/uri-list` 三平台
- ✅ 接收端剪贴板回灌（`FileSetCollector` + 4 类 skip conditions + pre-stamp 防回环 + `InboundFileApplyResult` typed error enum + `decide_reinject_skip` 纯函数 + 11 单测）
- ✅ TOML `[clipboard]` 段加 `inject_to_clipboard` 字段（4.1 落地）

**测试统计**：
- 491+344 lib tests pass / 0 fail / 1 pre-existing race-prone `#[ignore]`
- 闸 3 全套绿（cargo build / test / clippy / fmt）
- 0 P0 / 0 P1 / 2 P2（不阻塞）

**用户 M4 真机验证结果**（2026-09-13 17:30）：
- ✅ macOS / Linux 真机验证 PASS（剪贴板回灌双向 + 4 类 skip conditions 真机行为符合预期）
- ⚠️ Windows 真机验证 FAIL —— `set_files` Win32 路径 100% segfault（详见 SUGGESTION #S-12 + 2 份 BUG-INVESTIGATION 报告 `54053fe`）
- 用户决策：M4 通过；Windows BUG 推到 post-M5 hotfix 队列
- 用户已验证 release build 也崩 → 确认是 native crash（非 debug unwind）；具体根因未 pinpointed

**M5 启动项**：
- 派 `plan-step-executor` 执行 STEP-5.1（拔网处理：HTTP/3 stream error → IPC 推 `FrontendEvent::FileTransferFailed` + .partial 默认删除）
- STEP 偏差超过 45 min AI 时 LEADER 介入重拆
- 跨 STEP 影响的小问题记入 `next/SUGGESTION.md`
- 每完成一个 milestone LEADER 提交 git（commit message 格式英文，不带 M / STEP 编号）+ 更新 `next/.LEADER-STATE.md`

**M4 整批审 carry-forward**（2 P2 不阻塞，可后续清理）：
- **P2-1**：`src/service.rs:5897-5901` `handle_clipboard_inbound_files_decide` doc-comment 自相矛盾（"removed entirely" + "was kept `#[allow(dead_code)]`" 矛盾；实际已删除）
- **P2-2**：`src/service.rs:6481, 4051-4059` `ReinjectSkipReason::NoLandedPaths` 变体定义但实现中永不构造（defensive 分支直接 `return`）

> P2 留给后续 STEP（M5 / post-M5）由 executor 顺手清理；不立 SUGGESTION 条目（validator 已就地点评）

**M3a 完成交付清单**（21 commit since `a18dd5c` baseline）：
- ✅ 200 MiB 文件传输端到端通（SHA-256 一致）
- ✅ 源端取消接收端能响应（end-to-end ~10ms，远低于 1s 预算）
- ✅ HTTP/3 流式传输不爆内存（chunked write，PRIORITY_BULK=-100 复用 b4191d4 接线）
- ✅ `src/popup.rs` 模块新建 + `notify-rust = "4"` 依赖就绪（承接 commit `4ae86cf` 的 popup 框架到 main HEAD）

**测试统计**：
- 326 lib pass / 0 fail / 1 `#[ignore]` race-prone
- 481 workspace pass / 2 pre-existing flakes（input-capture macOS + http3 高负载 RTT）
- fmt 0 diff / 0 new clippy warnings

**用户真机验证清单**（PLAN-2 §8 M3a 人类测试矩阵）：
1. macOS Finder 复制 200 MiB 随机文件 → 对端 `/tmp/received/` 落盘 → `sha256sum` 一致
2. 同上，B→A 反向也跑一次（**双向验收约定**）
3. 100 Mbps LAN 实测 < 30s（A→B + B→A 各计时）
4. 源端立即覆盖剪贴板 → 接收端不应下载（cancel 触发）
5. 1 KiB / 1 MiB / 200 MiB 三种大小各跑双向
6. 复制 60 MiB 文件 → 桌面通知"超过上限，已跳过"+ 日志 "file exceeds limit" + 对端剪贴板无变化

**用户对齐下一里程碑（M4）前**：
- ✅ 验证 200 MiB 性能 + 取消语义真机通过
- ✅ 决策 M4 / M5 是否需要在 P2 carry-forward（5 项 P2 中 P2.1 / P2.3 是 M5 STEP-5.1 候补）
- ✅ 决策 M4 / M5 是否拆分 / 调整（PLAN §4 估时 M4 ~4.5h / M5 ~7.0h）

**不自动派 M4**（等用户决策）

**M4 STEP-4.3 ✅ 已完成（2 commit：a8db685 / 4d573dd）**：
- 实际 ~55 min AI
- 接收端剪贴板回灌：FileSetCollector + 4 类 skip conditions + pre-stamp 防回环 + IPC 集成
- InboundFileApplyResult 扩展 typed `error: Option<InboundFileError>` enum + `batch_fingerprint`
- 11 reinject_decision_tests + 6 apply_inbound_files_task 哨兵更新
- 6 PLAN 偏差（全部 A1 策略：typed enum / pure decision fn / async handle_inbound_files_applied / failure-also-push / string-prefix classification at boundary / 6 test call-sites）
- 0 新增 SUGGESTION
- 343 lib tests pass / 0 fail（闸 3 M4 收尾全套绿）

**M4 STEP-4.2 ✅ 已完成（4 commit：8e98c21 / 55c3c10 / ba21e60 / 0b08010）**：
- 实际 ~55 min AI
- `ClipboardBackend::set_files` trait + 三平台实现
- macOS NSPasteboard NSFilenamesPboardType + image_cache invalidation
- Windows CF_HDROP + DROPFILES payload + 4 单测
- Linux xclip/wl-copy text/uri-list + RFC 2483 build_uri_list + 3 单测
- 5 PLAN 偏差（全部 A1 策略：mock → helper 直测 / cast_unchecked / image_cache / 空 slice 防御 / percent-encoding follow-up 锚点）
- 0 新增 SUGGESTION
- 489+ lib tests pass / 0 fail

**M4 validator 整批审**（派发中）：
- 起点 commit：`49fd3c7`（M3a FULL validator 终点）
- 终点 commit：`4d573dd`（M4 STEP-4.3 docs）
- 覆盖 STEP：4.1 + 4.2 + 4.3
- 触发条件：累计 > 1h ✅ + M4 全部 3 STEP 完成 ✅
- 期望输出：`next/STEP-VALIDATION-P2-M4-FULL.md`

**M4 STEP-4.1 ✅ 已完成（4 commit：05035d8 / ab40836 / f287c67 / 4905dde）**：
- 实际 ~45 min AI
- IPC `ClipboardConfig` 8 字段 + drop `auto_accept_files` + accept_dir 升级为 required `PathBuf`
- Service getter 化 + log 全部 8 字段 + DEFAULT_MAX_FILE_SIZE re-export
- TOML `TomlClipboard` 8 字段 + Config getter + omit-on-default pattern
- 关闭 SUGGESTION #S-5 / #S-8
- 489 lib tests pass / 0 fail / 1 pre-existing race-prone ignore