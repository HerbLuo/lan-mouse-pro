# LEADER 状态 — 2026-09-08 18:30

> LEADER 维护。compact 后 / 跨 session 第一件事 Read 本文件恢复进度。
> 每次状态变更（PLAN 切换 / STEP 完成 / validator 跑完 / milestone 完成 / 切换里程碑）更新。

---

## 当前 PLAN

`next/PLAN-1-POSITION-MULTI-MONITOR.md` — 多屏友好的被控端定位（**M3 引入型回归 bug 已定位，3.4-3.5-3.6 STEP 草稿已写完，等待用户决策后启动执行**；M4 待启动）

> 关键状态变更：用户 2026-09-07 21:30 启动 M3 真机回归，发现 macOS / Windows 双屏下鼠标无法切边；leader 定位 bug → planer 改写 PLAN 加 3.4-3.5-3.6（planer 报告 task `ad83156819f448804` 完成）。

---

## 当前 PLAN

`next/PLAN-1-POSITION-MULTI-MONITOR.md` — 多屏友好的被控端定位（**M3 已完成**；M4 待启动）

> 未来可能存在的其他 PLAN：PLAN-A / PLAN-B / ...
> 切换 PLAN 时：更新本字段 + 把"已完成 STEP / 累计执行时间 / 上次 validator 位置"全部重置

---

## 当前里程碑

**M3 — 显示器绑定 UI + 落地回归修复（⚠️ 新 BUG：macOS scale=2 内建屏 monitor 配置不穿）**

> 2026-09-08 18:30 用户新 BUG：macOS master + 双屏（内建 Display1 scale=2 1512x982 / 外置 Display3 scale=1 3440x1440）
> 配置 client monitor=Display3（外置）→ 正常穿 ✅
> 配置 client monitor=Display1（内建 Retina）→ 不穿 ❌
> 怀疑：macOS CGEvent (point) vs CGDisplay bounds (pixel) 坐标系错位 + scale=2 转换
> leader 派调研 agent（agentId: `a64b743577a549aa7`）静态读 macos.rs / geometry/mod.rs / 已加 fixture 单测覆盖矩阵

> M3.1-3.6 已完成（9 commit）。真机回归暴露的 4 个 backend 引入型回归 bug 全部修复：
> - **macOS** ✅：`crossed_pure` 抽离 + `DisplayBound` 字段保留 + `build_display_bounds` 单源
> - **Windows** ✅：`activation_pure` 抽离 + `DISPLAYS: Vec<DisplayBound>` 升级 + `query_pure` 共享 helper
> - **layer_shell** ✅：`Capture::create` / `destroy` 接完整 `&BarrierKey`（不再 `from_pos` 剥字段）
> - **libei** ✅：`select_barriers` 重构 + `select_barriers_with_monitor_field` 单测锁住天然兼容语义
>
> 已完成：M0 / M1 / M2 / M3.1-3.6（9 commit）；真机回归用户侧待做
> M3 validator ✅ PASS-with-followup（0 P0/P1 / 4 P2 micro-cleanup 已清空）

---

## 已完成 STEP

### M0（已完成）

- M0 全步骤 ✅（commit: b38cb6f, ab0e076, 2841d54）

### M1（已完成，commit: 08cb3a3）

- M1.STEP-1.1 ✅ BarrierKey 数据结构 + InputCapture 重键（~26 min）
- M1.STEP-1.2 ✅ 五个 backend 完整迁移（~14 min）
- M1.STEP-1.3 ✅ CaptureTask / service 集成（~45 min，超 PLAN 30 min；PLAN 偏差 #1 `State::Pending` 保留 `started`）
- M1.STEP-1.4 ✅ fmt + clippy M1 scope 收尾（~20 min）
- M1.validator ✅ PASS-with-followup
- M1.commit ✅ `M1: barrier key data model refactor`

### M2（✅ 已完成，9 commit，65 个新单测全绿）

- M2.STEP-2.1 ✅ MonitorInfo + IPC FrontendEvent::MonitorsChanged/BindingInvalid + 9 单测（commit: 61ff247，~25 min）
- M2.STEP-2.2 ✅ macOS enumerate_monitors + IOKit FFI + watch channel + 8 单测（commit: 35c9e32，~45 min）
- M2.STEP-2.2-fixup ✅ IOKit fallback id 唯一性 P1 修复 + 删 last_monitors + UTF-8 扩 accented Latin + 3 单测（commit: 1bfaa45，~25 min，token plan 上限后由 leader 接力）
- M2.STEP-2.3 ✅ Windows enumerate_displays + EnumDisplayDevicesW + 14 单测（commit: 3f9f560，~30 min）
- M2.STEP-2.4 ✅ Linux layer_shell + libei 枚举 + 24 单测（commit: 60025ff，~55 min，含 1 code-review perf fix）
- M2.STEP-2.4-fixup ✅ libei gate 位置 P1 修复 + publish_monitors_if_changed helper + 3 单测（commit: dc7e747，~25 min）
- M2.STEP-2.5 ✅ Capture trait 加 monitors() + 5 backend 实现 + InputCapture 转发 + 1 单测（commit: 74949d4，~20 min）
- M2.STEP-2.6 ✅ service reconcile + 8 单测（mock 移除/几何变化/no-op/启动 seed/混合 binding/镜像转换）+ Vue 2 变体 + ConnectionRow 高亮 + tooltip（commit: 63706b5，~50 min，1 Hz poll 是临时方案；SUGGESTION #1 已接受）
- M2.STEP-2.7 ✅ M2 收尾 fmt/clippy/lint/type-check/test 全绿 + 真机 macOS 双屏验证 seed 2 monitors + IOKit fallback id 唯一 + mixed-DPI scale=2/1 + manual checklist 3 平台（commit: d08218d，~25 min）
- M2.validator-整批 ✅ PASS（0 PLAN 偏离 / 0 REQUIREMENT / 0 P0-P1 BUG / 2 P2 cosmetic comment drift 留 micro-cleanup backlog；4 项 M2 交付全到位；65 个新单测全绿；M2 范围 fmt-clean + 0 clippy warning）

### M3（✅ 已完成，3 commit：54a88ad / f0e56fc / 9b316aa；12 新单测全绿 + 23 vitest case 全绿；0 PLAN 偏差 / 0 P0-P1 BUG）

- M3.STEP-3.1 ✅ 后端链路（commit: 54a88ad `feat: bind clients to specific monitors`，~40 min；12 新单测全绿，143 → 155 cargo pass；0 PLAN 偏差）
- M3.STEP-3.2 ⚠️ 前端源码 + 测试落地；pnpm 三件套被网络阻断（~55 min；SUGGESTION #5 暴露）
- M3.STEP-3.2-FIXUP ✅ 代理补验（commit 未提，被 FIXUP2 合入；~10 min；HTTP 代理 `127.0.0.1:8118` 解决 npmjs.org 直连 5s timeout）
- M3.STEP-3.2-FIXUP2 ✅ 修 #6 applyEvent export + #7 monitor normalize + #8 noUncheckedIndexedAccess（被合入 commit f0e56fc；~15 min；vitest 17/23 → 23/23）
- M3.validator ✅ PASS-with-followup（2 P2 仅文档数字漂移，无代码 BUG）
- M3.STEP-3.3 ✅ M3 wrap-up（commit: 9b316aa `chore: M3 wrap-up`；leader 接力完成因 executor token plan 上限提前中断；L1 终态：cargo fmt 0 diff / clippy 0 warning / Rust 143 pass / pnpm build ✅ / vitest 23/23 / oxlint 0 issue）
- M3.commit ✅ `feat: bind clients to specific monitors` + `feat(vue): monitor dropdown for client binding` + `chore: M3 wrap-up`

### M3 修复 STEP（✅ 已完成，10 commit；4 backend monitor 字段传递 bug 全部修复 + H1 fallback invariant 错位 + 0 P0/P1 BUG；165 cargo pass / 0 fail）

- M3.STEP-3.4 ✅ BarrierKey 字段保留修复（commit: `d7d2c58` ~50 min + 越界拆分 `7ed1352` ~5 min + docs `3286a6d` / `43b10a1` / `107476d`）
- M3.STEP-3.5 ✅ Windows activation_pure 抽离 + libei sanity（commit: `3afeeff` ~70 min + report `bcc40c7`；executor 撞 429，leader 接力 commit）
- M3.STEP-3.6 ✅ L1 wrap-up + P2 backlog 清空（commit: `9dcbe1b` ~15 min + doc `165d998`）
- M3.validator ✅ PASS-with-followup（0 P0/P1 / 4 P2 全部 micro-cleanup，P2 backlog 已清空）
- **M3.STEP-3.4-FIXUP** ✅ H1 query_pure legacy fallback invariant 修复（commit: `f7a3ab8` ~15 min + report `280209e`）
  - 必要 PLAN 偏差 #1：C3/W3 测试 assertion 反转（原 assertion 即 H1 暴露的 buggy 行为）+ 新增 production-realistic 单测
  - H2/H3 仍是次要嫌疑（如真机回归仍失败按 H2→H3 排查）
- M3.commit-fix ✅ `fix(input-capture): preserve monitor_id in barrier key across 3 backends` + `fix(service): re-broadcast monitor snapshot on frontend (re)connect` + `fix(input-capture): pure barrier key query on Windows + libei sanity refactor` + `chore(input-capture): clear validator P2 backlog` + `fix(geometry): legacy client monitor=None fallback in query_pure`

### M3.4-3.6 修复 STEP 草稿（planer 改写 PLAN 完成，✅ 决策完成 + §2/§4 同步已落地）

- planer 任务 ID：`ad83156819f448804`（2026-09-07 22:15 完成；3.4-3.6 STEP 草稿已写入 §3 + §4 + §8）
- planer 任务 ID 2：§2 同步（2026-09-08 11:30 完成）
  - 改动：§2 line 62 M3 行估时 `~1.5h` → `3.5h` + 状态 `待启动` → `进行中`；§2 line 64 合计 `~16h` → `~18h`
  - 审阅：1 轮（3 个并发 sub-agent 一致结论；planer 自报"误发起 3 次相同审阅，浪费；后续提醒=并发=1"）
  - 跨章节契约修复：§4 line 226 M3 状态 `待启动` → `进行中（含 3.4-3.6 修复）`（leader 决策，选项 A；planer 未擅改）
- 用户决策（2026-09-08 11:30 收到）：**layer_shell 同形 bug 合并进 STEP-3.4**（选项 A）

### M3.4（✅ 已完成，3 个 commit：d7d2c58 / 7ed1352 / 3286a6d / 43b10a1；16 新单测全绿 + 0 P0/P1 BUG）

- M3.STEP-3.4 ✅ BarrierKey 字段保留修复（macOS + layer_shell + Windows DISPLAYS 字段升级；commit: d7d2c58，~50 min）
  - PLAN 偏差 #1（Windows DISPLAYS 升级前置到 3.4 — 为 3.5 抽 `activation_pure` 提供类型基础；已在 prompt 层批准）
  - 16 新单测：9 geometry `crossed_pure_4x2` 矩阵 + 4 macos `build_display_bounds_pure` + 3 layer_shell `capture_create_preserves_monitor`（cfg gate）
  - 旧 macOS / Windows / libei / dummy 单测零回归
- M3.STEP-3.4-附带 ✅ WS 重连 re-broadcast（commit: 7ed1352，~5 min；executor 越界但功能合理，独立 commit）
  - 修 M3.1 引入的"seed MonitorsChanged 在 WS 连接前已发出 → GUI 启动 dropdown 为空，要等 2s poll"问题
  - 不在 3.4 表格范围但同 milestone 收尾，leader 决定**拆 commit** 让 git history 清晰
- M3.commit-doc ✅ `docs: sync PLAN-1 §2/§4 M3 status; record 3.4 backend fix in LEADER-STATE`（commit: 3286a6d）
- M3.commit-doc2 ✅ `docs: add STEP-M3-3.4 executor report`（commit: 43b10a1）
- 累计耗时：~60 min（含 planer §2 同步 ~4 min + leader 决策 ~6 min + executor ~50 min）

### M3.5（✅ 已完成，commit: 3afeeff / bcc40c7；6+ 新单测全绿；executor 撞 token 上限后 leader 接力 commit）

- M3.STEP-3.5 ✅ Windows `activation_pure` 抽离 + libei `select_barriers` 重构 + sanity 单测（commit: 3afeeff，~70 min：executor 跑 ~60 min 撞 429，leader 接力 commit + 报告 ~10 min）
  - **代码完成度 100%**：3 文件改完（geometry +291 / libei +206 / event_thread +54），本机 cargo 全绿（geometry 42 pass / 其它 68 pass / 零回归）
  - 6 个新 `activation_pure_w1-w6`（Windows 激活矩阵）+ `select_barriers_with_monitor_field` + 2 边界 case
  - **libei cfg gate 限制**：libei 单测本机 macOS 不编（仅 target_os = "linux" 编译）；代码已就位 + executor Linux target check 通过，**真机验证推迟到 3.6 用户真机回归**
- M3.commit-doc ✅ `docs: add STEP-M3-3.5 report`（commit: bcc40c7）
- 累计耗时（M3.5）：~70 min

### M3.4+M3.5 validator 整批审阅（✅ PASS-with-followup）

- 报告文件：`next/STEP-VALIDATION-M3-3.4-3.5.md`
- **Verdict**：✅ PASS-with-followup
- 0 P0 / 0 P1 / **4 P2 全部 micro-cleanup**（不阻塞 M3）
- **核心确认**：4 backend monitor 字段传递 bug 全部正确修复；`query_pure` 共享 helper 消除 macOS/Windows 半开约定 drift 风险；25 个新单测 + 旧 143 → 168 零回归；Cargo.toml / Cargo.lock 不变；service.rs WS 重连时序正确
- **4 P2 backlog**（全部已清空）：
  - P2.1 ✅：`display_containing_idx` docstring 标题 + PLAN §3 C4 文字（commit `165d998`）
  - P2.2 ✅：`libei.rs:82-91` `pos_to_barrier` dead code（commit `9dcbe1b`）
  - P2.3 ✅：`macos.rs:557` `enumerate_monitors(&[DisplayBound])` 改为无参 + 4 caller + 1 测试（commit `9dcbe1b`）
  - P2.4 ✅：STEP-M3-3.4.md L1 表格 cargo test 行（commit `107476d`）

---

## 当前累计执行时间（自上次 validator 审阅起）

~190 min（planer §2 同步 ~4 min + leader 决策 ~6 min + STEP-3.4 ~50 min + STEP-3.5 ~70 min + validator ~10 min + leader 接力 ~50 min）— **远超 1h 阈值**

> 触发 validator 的两个条件（OR）：
>
> - 累计 > 1 小时 ✅ 已达
> - 当前里程碑结束（M3 全部 3.4-3.5-3.6 完成） — 仍差 3.6
>
> 决策：3.4+3.5 整批 PASS ✅ → P2 backlog 收尾中 → 派 STEP-3.6（fmt/clippy/test + 顺手清 P2.2/P2.3 + 修 P2.1 docstring 标题）

---

## 上次 validator 审阅位置

M3 修复 STEP（3.4+3.5）整批审阅 ✅ PASS-with-followup（commit `3afeeff` 为终点；0 P0/P1 BUG；4 P2 micro-cleanup backlog；4 backend monitor 字段传递 bug 全部正确修复；`query_pure` 共享 helper 验证零 drift）

---

## 待办 / 决策项

### 用户决策（✅ 已收齐）

- [x] **用户**：layer_shell 同形 bug 范围决策 → 选项 A 合并进 STEP-3.4
- [x] **用户**：PLAN-1 §2 M3 行是否同步 → 同步（同时顺手修了 §4 line 226 状态列、§2 line 64 合计列）
- [x] **用户**：派 STEP-3.4 executor → 已派并完成（agentId: `a88f62fd6930e8377`）

### 用户决策（M3 → M4 桥梁，原待办）

- [ ] **用户**：真机回归 + 3.4-3.6 全部跑完后，**对齐是否启动 M4 — 暴露边段 + 画布编辑器**
  - M4 目标：后端按真实几何算出暴露边段；前端用 SVG 画布可视化；用户点/拖 client 到具体边段
  - M4 估时 ~9h（PLAN §4）
  - M4 已知限制：Wayland 合成器对 sub-pixel surface 接受度差异（KDE/Sway/Hyprland 逐个验证）；libei portal 后端不支持 sub-region 自动降级为 monitor 全边
  - **M4 完成时 = 产品形态达标**（与 ShareMouse/Deskflow 同级 UX）

### 已通过

- [x] M1 单屏 GUI 端到端回归（用户 2026-09-06 18:35 确认）
- [x] M2 真机 macOS 双屏 seed 验证（2026-09-07，2 monitors + IOKit fallback id 唯一 + mixed-DPI scale=2/1）
- [x] M2 三平台 manual checklist（用户 2026-09-07 19:30 确认）
- [x] M3 validator 累积 4 STEP 审阅 PASS-with-followup（2026-09-07 21:00，0 P0-P1）

### P2 cosmetic / tech debt backlog（不阻塞，留 micro-cleanup）

- `macos.rs:556-581` read_display_info doc 注释重复
- `geometry/mod.rs:255` MonitorInfo 镜像惯例 From trait 自动生成
- `libei.rs:1205-1209` 测试 doc comment overstate 范围
- STEP-2.5 executor 自报 6 个 minor finding
- 5 pre-existing clippy errors in `src/connect.rs` + `src/quic_transport/*`（与 M2 无关）
- 24 pre-existing fmt diffs in non-M2 files（与 M2 无关）
- M2.validator-整批 新增 2 项 comment drift：
  - `src/service.rs:994` `reconcile_tests` mod 头注释引用不存在的 end-to-end 测试模块
  - `src/capture.rs:598-604` `do_capture` 头注释 `last_monitors is reset to empty inside do_capture_session` 与实际代码不符

---

## 下一步

**M3 反向 Enter 排查 + 修复**：
1. ✅ 用户贴 INFO 日志（不是 trace 但足够定位）
2. ✅ leader 静态读代码锁定 **H6 真根因 ~99%**：
   - `emulation.rs:173` 收到 Enter 时 `send EmulationEvent::ReleaseNotify`
   - ReleaseNotify handler 调 `release_capture`，**force-reset state + 调 capture.release()**
   - capture.release() 销毁所有 OS-level barrier，包括 `service.rs:593 add_incoming` 路径里建的 EnterOnly 反向 barrier
   - 反向触发失去物理基础 → cursor 越过边无响应 → 两边都无日志
3. ⏳ 调研 agent `ae5fa2357e927f7a6` 二次验证 H6 + 提议修复范围
4. → 派 plan-step-executor 修：
   - 让 ReleaseNotify 路径**只销毁 active barrier，保留 EnterOnly barrier**（最小改动）
   - 或者：EnterOnly barrier 用独立 OS-level object 不与 active barrier 共享
5. → 修复完 commit + 移除临时 debug log
6. → 真机再回归 → M3 真完成 → 决策 M4 启动
7. **附带**（独立 fixup）：主控 dial 慢（BeginPending 循环 2.5s 延迟）→ 用户是否要拆 fixup

**leader 待办**：等调研 agent 回报 → 决定修复方案 → 派修。
