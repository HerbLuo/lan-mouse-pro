# LEADER 状态 — 2026-09-08 22:30

> LEADER 维护。compact 后 / 跨 session 第一件事 Read 本文件恢复进度。
> 每次状态变更（PLAN 切换 / STEP 完成 / validator 跑完 / milestone 完成 / 切换里程碑）更新。

---

## 当前 PLAN

`next/PLAN-2-CLIPBOARD.md` — **QUIC 传输就绪 + 跨设备剪贴板 / 文件同步**（M0a / M0b / M0c 完成；⏳ M1a 待启动）

> 用户 2026-09-08 19:30 指示 M4 暂延；2026-09-08 20:00 启动 PLAN-2
> PLAN-2 文档 597 行已就绪（M0a / M0b / M0c / M1a / M1b / M2a / M2b / M3a / M3b / M4 共 10 个里程碑；STEP 0.0-0.7 / 1a / 1b / 2a / 2b / 3a / 3b / 4 已写好；评审 #1-#6 已记录）
> 切换 PLAN 时重置"累计执行时间 / 上次 validator 位置"

> 已完成：PLAN-1（多屏友好的被控端定位）— M0-M3 全完成、M4 暂延（用户决策 2026-09-08 19:30）

---

## 当前里程碑

**M0c — StreamC 接通 + IPC 扩展（✅ 全部 STEP 完成）**

> PLAN-2 §2 路线图：M0a ✅ → M0b ✅ → **M0c ✅** → M1a ⏳ → M1b → M2a → M2b → M3a → M3b → M4
> M0c 5 commit（`371eee0` / `47a4e03` / `44dbd60` / `3d5ae1f` / `7282756`）
> 170 cargo pass / 0 fail；0 new clippy（M0a/M0b/M0c 引入）；pre-existing 12 clippy error 按 scope discipline 不修
> StreamC 端到端真的工作（full-QUIC-stack 单测 `stream_c_clipboard_text_round_trip`）；"Stream C is M0c-only" 警告消失
> 累计执行 ~285 min（PLAN-2 重置后）

### M0c 交付清单
- ✅ lan-mouse-proto v0.4.0（含 7 个新 ProtoEvent 变体 + FixedCodec/VarCodec 双轨 + wire-compat 验证）
- ✅ http3.rs（HTTP/3-lite over bare QUIC bidi streams + Semaphore 32 in-flight 上限）
- ✅ examples/h3_pingpong.rs（4 场景 spike：/healthz / 200 MiB / cancel / 拔网）
- ✅ StreamC 接通（send + read + dispatch）+ listen.rs 调 `start_http3_server`
- ✅ IPC 扩展：`ClipboardConfig` (daemon-global) + `ClientConfig.enable_clipboard_to` (per-peer) + `FrontendEvent::ClipboardState`
- ✅ Path 1 → Path 2 决策记录在 SUGGESTION-FIXED #9（h3-quinn ALPN 单端口不可路由 → 自实现精简协议）
- ✅ Validator P2 backlog 已清：P2.1 误报忽略 / P2.2 Semaphore 修 / P2.3 押后

---

## 当前累计执行时间（自上次 validator 审阅起）

~95 min（自 M0b validator 完成后起算；M0c + post-Leader Ack demotion 累计）

> 触发 validator 的两个条件（OR）：
> - 累计 > 1 小时 ✅ 已达
> - 当前里程碑结束（M0c 全部 STEP 完成）✅ 已达
>
> 下次触发时机：M0c validator 完成 OR 累计 > 1h

---

## 上次 validator 审阅位置

M0b 整批审阅（commit `f66c0f7` / `8729f9f` / `ff2f55c` / `98dd971` / `9acdf21` / `fdf03bc`）✅ PASS-with-followup（`next/STEP-VALIDATION-P2-M0b.md`；0 P0/P1 / 3 P2 / 3 P3）
**当前目标**：M0c 整批审（commit `371eee0` / `47a4e03` / `44dbd60` / `3d5ae1f` / `7282756` + `86781b0` post-Leader Ack demotion）

---

## 待办 / 决策项

### 用户决策（PLAN-2 启动后）

- [x] **用户**：启动 PLAN-2 → ✅ 2026-09-08 20:00
- [x] **用户**：派 STEP-0.0+0.1 合并执行 → ✅
- [x] **用户**：commit 拆分方式（fmt-only / 真逻辑 / 报告 3 拆）→ ✅ leader 决策
- [x] **用户**：Path 1 vs Path 2 选择 → executor 自决（Path 2 已落）
- [x] **用户**：M0a/M0b validator PASS-with-followup 接受 → ✅
- [x] **用户**：M0b P2 backlog 处理方式 → ✅ P2.1 误报忽略 / P2.2 必修 / P2.3 押后
- [x] **用户**：post-Leave Ack WARN demote → ✅ trace level（commit `86781b0`）

### 用户决策（待办）

- [ ] **用户**：真机跑 `curl --http3 https://<peer>:4252/healthz` 验证 M0c 端到端（PLAN §3 M0c STEP-0.7 人类侧）
  - 场景 A：本机 loopback（macOS daemon + h3-pingpong client on 127.0.0.1）
  - 场景 B：两台真机 LAN 跨机
  - 场景 C：跨平台（macOS daemon + Linux curl --http3）
  - **不阻塞** M0c commit（full-QUIC-stack 单测已通过）+ 不阻塞 M1a 启动
- [ ] **用户**：派 M1a（剪贴板小文本 ≤ 1 KiB 同步）→ leader 准备好后派发
- [ ] **用户**：派 M0c validator 整批审 → 与 M1a 并行（不阻塞）

### Hot-fix（用户本地记录）

> 用户在 `next/.LEADER.md` 维护 hot-fix v4/v5 记录（commit `bb509a7` / `8f55318` 占位 hash）。
> 这些是 PLAN-1 M3 期间的真机回归 hot-fix，**由你本地记录**，leader 不主导。
> 实际 git history 中无 `bb509a7` / `8f55318`；对应的"记录 commit"是 `9777073`（v4 记录）+ `9b2a405`（v5 记录）。

---

## 已完成 STEP（PLAN-2）

### M0a — ProtoEvent codec 双轨化（✅ 已完成，5 commit）

- **M0a.STEP-0.0+0.1 合并** ✅
  - lan-mouse-proto bump 0.3.0 → 0.4.0
  - 新增 `lan-mouse-proto/src/codec.rs`（FixedCodec / VarCodec trait + dispatcher）
  - 7 个新 ProtoEvent 变体：`ClipboardText` / `ClipboardImage` / `ClipboardFiles` / `FileTransferOffer` / `FileTransferResponse` / `FileTransferCancel` / `ClipboardRequest`
  - `EventType` enum + 顶层 `From<ProtoEvent> for Vec<u8>` / `TryFrom<&[u8]> for ProtoEvent`
  - `route_input` 加 StreamC 分支（protocol.rs）
  - `write_hello_frame` / `write_frame` 改 `event.clone().into()`（ProtoEvent 失去 Copy 后的必要改动）
  - 33 新单测（13 codec.rs + 14 lib.rs + 6 protocol.rs）
  - **wire-compat 验证**：新 EventType 全部不编码在 StreamA；M0a 不接通 StreamC（旧 daemon 完全兼容）
  - **累计耗时**：~45 min（executor 撞 429 后 leader 接力 build/test/commit ~15 min）

| Commit | 标题 |
|---|---|
| `a71340a` | chore(fmt): whitespace fold + .clone() on ProtoEvent de-Copy |
| `248d276` | feat(proto): dual-track ProtoEvent codec + clipboard / file-transfer variants |
| `f21ae81` | docs: add STEP-P2-M0a report (leader-continued after executor 429) |
| `a032e75` | docs: record M0a completion (3 commits; executor 429 → leader continued) |

- **M0a.validator** ✅ PASS-with-followup（`next/STEP-VALIDATION-P2-M0a.md`）
  - 0 P0 / 0 P1 / 2 P2 / 2 P3
  - 224 cargo pass / 0 fail（spot-check 验证）
  - P2.1 ✅ commit 卫生（amend message "whitespace fold + .clone() on ProtoEvent de-Copy"）
  - P2.2 ✅ doc drift 修正（Channel enum + route_input doc 更新到 M0a 7 新变体）

### M0b — HTTP/3 基础（✅ 已完成，6 commit）

- **M0b.STEP-0.2** ✅ h3 spike + Path 1 → Path 2 决策
  - `src/quic_transport/http3.rs`（~580 行）：HTTP/3-lite over bare QUIC bidi streams
  - `examples/h3_pingpong.rs`（~280 行）：4 场景 spike 全绿
  - wire framing：`[u16 method_len][method][u16 path_len][path][u32 body_len][body]`
  - ALPN 保持 `b"lan-mouse"`（单端口不变）
  - Path 1（h3-quinn ALPN）放弃：单端口不可路由 → Path 2 自实现精简协议
- **M0b.STEP-0.3+0.4 合并** ✅ server accept loop + Http3Client helper
  - Router / default_router / Server / Http3Client / GrowingSink
  - `PeerSession::start_http3_server(router)` 公共方法
  - 16 新单测 + 11 spike 单测 = 27 http3 单测

| Commit | 标题 |
|---|---|
| `f66c0f7` | feat(quic): HTTP/3-lite over bare QUIC bidi streams (M0b STEP-0.2 Path 2) |
| `8729f9f` | chore(deps): add bytes / h3 / h3-quinn / rand for M0b HTTP/3 spike |
| `ff2f55c` | docs(suggestion): record Path 1 → Path 2 decision rationale in FIXED #9 |
| `98dd971` | docs(quic): update Channel enum + route_input doc for M0a StreamC routing (validator P2.2) + add STEP-P2-M0b-0.2 report |
| `9acdf21` | feat(quic): http3 server accept loop + Http3Client helper (M0b STEP-0.3+0.4 merge) |
| `fdf03bc` | docs: add STEP-P2-M0b-0.3-0.4 report (server accept loop + client helper merge) |

- **M0b.validator** ✅ PASS-with-followup（`next/STEP-VALIDATION-P2-M0b.md`）
  - 0 P0 / 0 P1 / 3 P2 / 3 P3
  - 27 http3 单测 + 4 spike 场景全绿
  - P2.1 误报（Cargo.toml bundle 段是用户 commit `618b4b1` 的内容，非 M0b 引入）— leader 忽略
  - P2.2 ✅ server accept loop 无并发上限 → M0c `47a4e03` 加 `Arc<Semaphore>` 32 in-flight
  - P2.3 押后（spawn 风格分裂，微优化）

### M0c — StreamC 接通 + IPC 扩展（✅ 已完成，5 commit）

- **M0c.STEP-0.5a+0.5b+0.6 合并** ✅
  - PeerSession 加 `cached_send_c: Mutex<Option<SendStream>>` + `send_stream_c(event)`
  - StreamEvent enum 加 `ClipboardMeta(ProtoEvent)` 变体
  - `streams::read_loop` 替换 `drop(bunch.c)` 占位 → `read_stream_c_loop`（var-codec decode + forward）
  - `listen.rs::handle_quic_peer_supervisor` 调 `peer.start_http3_server(default_router())`（server 真的起来）
  - IPC：`ClipboardConfig` (daemon-global) + `ClientConfig.enable_clipboard_to` (per-peer) + `FrontendEvent::ClipboardState`
  - 20 新单测（16 StreamC round-trip + 1 prefix-match edge + 3 IPC serde round-trip）
- **M0c.STEP-0.7** ✅ fmt + clippy + 三平台 + 真机 curl 文档化
  - fmt 0 diff / 170 cargo pass / 0 fail
  - pre-existing 12 clippy error 按 scope discipline 不修（M0a/M0b/M0c 引入 0）
  - 真机 curl 命令文档化在 STEP-P2-M0c-0.7.md（场景 A/B/C 任一）

| Commit | 标题 |
|---|---|
| `371eee0` | feat(quic): wire StreamC reader + http3 server in listen.rs + ClipboardConfig IPC (M0c STEP-0.5a+0.5b+0.6 merge) |
| `47a4e03` | fix(quic): cap http3 server concurrent in-flight requests at 32 (validator M0b P2.2) |
| `44dbd60` | docs: add STEP-P2-M0c-0.5-0.6 report (StreamC end-to-end + IPC) |
| `3d5ae1f` | docs: archive STEP-VALIDATION-P2-M0b (PASS-with-followup; 0 P0/P1 / 3 P2) |
| `7282756` | docs: add STEP-P2-M0c-0.7 report (leader-continued after executor 529; 170 pass / 0 fail; pre-existing clippy scope decision) |

### PLAN-2 M0a/M0b/M0c 完成后 micro-cleanup（leader 直修，~5 min）

- **`86781b0`** ✅ `chore(capture): demote post-Leave Ack warning to trace level`
  - 用户真机反馈：每次反向切换触发 `WARN client N acknowledged -- but no active_client (stale Ack after timeout, ignoring)`
  - **根因**：post-Leave Ack race（T0 master 发 Leave → T1 master release_capture 清 active_client + state=Idle → T2 slave 收 Leave + 发 Ack → T3 master 收 Ack 时 active_client 已 None）
  - **不是 timeout**，是预期路径；WARN 级别太高
  - 降级到 `log::trace!` + 重写 message 解释 race + 详尽注释说明历史
  - 170 cargo pass / 0 fail 仍保持

---

## 已完成 STEP（PLAN-1 历史摘要；不展开）

> PLAN-1（多屏友好的被控端定位）已收尾：M0（macOS 正确性）+ M1（BarrierKey 重构）+ M2（显示器枚举 + 热插拔）+ M3.1-3.6（绑定 UI + 4 个 hot-fix）全部完成。M4 暂延（用户决策 2026-09-08 19:30）。
> 详细 STEP 记录已 commit 在各自 commit message + STEP-VALIDATION-*.md 报告。

### PLAN-1 累计成果
- 26 commit（M0 3 / M1 4 / M2 7 / M3 16 含 hot-fix）
- 176 cargo pass / 0 fail（M3 终点；之后 +10 = M3.4 +5 + M3.5 +6 + M3.6 +5 - 4 overlap）
- 4 backend monitor 字段传递 bug + H6 release_capture + moved_across_boundary + hot-fix v4/v5 全部修好
- 用户真机正向 / 反向 / D1.Left 全部验证通过

### PLAN-1 / PLAN-2 接力点（2026-09-08）
- 19:30：用户指示 M4 暂延
- 20:00：启动 PLAN-2（重置累计时间 + 上次 validator 位置）
- 20:35：planer 已就绪（PLAN-2 文档 597 行 pre-existing，无需重写）

---

## 下一步

**派 M0c validator 整批审（5 commit）+ 派 M1a**：

1. **M0c validator**（与 M1a 并行）
   - 审范围：commit `371eee0` / `47a4e03` / `44dbd60` / `3d5ae1f` / `7282756` + `86781b0`
   - 重点：StreamC 端到端真的工作（不是只编译过）/ IPC 扩展向后兼容 / P2.2 Semaphore 修复质量
2. **M1a**（剪贴板小文本 ≤ 1 KiB 同步）
   - 真机测试：复制一段短文本 → 对端剪贴板出现
   - 估时 ~6h（PLAN §2 M1a 行）
3. **用户真机 curl --http3**（不阻塞）
   - 场景 A/B/C 任一验证 M0c 端到端

**leader 待办**：派 M0c validator + M1a executor（并行；不互相阻塞）。

**累计执行时间**：~285 min（PLAN-2 重置后）。
